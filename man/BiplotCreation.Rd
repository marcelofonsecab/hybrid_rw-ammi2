% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biplot_graph.R
\name{BiplotCreation}
\alias{BiplotCreation}
\title{Create a Biplot from SVD Results}
\usage{
BiplotCreation(
  SVD,
  env.names = NULL,
  gen.names = NULL,
  nms = c("IPC1", "IPC2"),
  lims = FALSE,
  lims.U,
  lims.V
)
}
\arguments{
\item{SVD}{An SVD object (list with elements \code{u}, \code{d}, and \code{v}).}

\item{env.names}{Optional character vector for environment names. Defaults to generated names.}

\item{gen.names}{Optional character vector for genotype names. Defaults to numeric indices.}

\item{nms}{A character vector of length 2 for labeling the axes. Default is \code{c("IPC1", "IPC2")}.}

\item{lims}{Logical; if \code{FALSE}, axis limits are automatically calculated. Default is \code{FALSE}.}

\item{lims.U}{Optional numeric vector for x-axis limits.}

\item{lims.V}{Optional numeric vector for y-axis limits.}
}
\value{
Produces a biplot on the current graphic device.
}
\description{
This function creates a biplot using scores and loadings extracted from an SVD object.
The scores and loadings are scaled by the square roots of the singular values.
}
\examples{
sim_data <- sim_amb(seed = 123)
ammi_res <- ammi_model(sim_data, Ncomp = 2)
BiplotCreation(ammi_res$SVD, env.names = levels(sim_data$env), gen.names = levels(sim_data$gen))

}
