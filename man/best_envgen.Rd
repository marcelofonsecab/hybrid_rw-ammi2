% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/best_envgen.R
\name{best_envgen}
\alias{best_envgen}
\title{Select the Best Genotype for Each Environment}
\usage{
best_envgen(x, quant = 1, gen_names0 = NULL, env_names = NULL, colors = FALSE)
}
\arguments{
\item{x}{An object containing an SVD result (with elements \code{SVD}).}

\item{quant}{An integer indicating which best genotype to select (1 for best, 2 for second best, etc.). Default is 1.}

\item{gen_names0}{Optional original genotype names. Default is \code{NULL} (will generate numeric names).}

\item{env_names}{Optional environment names. Default is \code{NULL} (will generate default names).}

\item{colors}{Logical; if \code{TRUE}, returns colored output. Default is \code{FALSE}.}
}
\value{
A matrix (or character matrix if \code{colors} is TRUE) indicating the best genotype for each environment.
}
\description{
This function identifies the best (or second best, etc.) genotype for every environment
based on projections derived from SVD results.
}
\examples{
sim_data <- sim_amb(seed = 123)
ammi_res <- ammi_model(sim_data, Ncomp = 2)
best_geno <- best_envgen(ammi_res, quant = 1)
best_geno

}
