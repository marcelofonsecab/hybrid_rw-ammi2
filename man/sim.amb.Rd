% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulations.R
\name{sim.amb}
\alias{sim.amb}
\title{Simulate Genotype-Environment Data}
\usage{
sim.amb(
  seed = NULL,
  Ngen = 100,
  Nenv = 8,
  Ncomp = 2,
  effectGlobal = c(mean = 15, sd = sqrt(3)),
  effectGen = c(mean = 5, sd = 1),
  effectEnv = c(mean = 8, sd = sqrt(2)),
  k = rep(1, Nenv)
)
}
\arguments{
\item{seed}{Optional seed for reproducibility. If \code{NULL}, a random seed is generated.}

\item{Ngen}{Number of genotypes. Default is 100.}

\item{Nenv}{Number of environments. Default is 8.}

\item{Ncomp}{Number of principal components to use for the multiplicative effects. Default is 2.}

\item{effectGlobal}{A numeric vector of length 2 (mean and standard deviation) for the global effect.
Default is \code{c(mean = 15, sd = sqrt(3))}.}

\item{effectGen}{A numeric vector of length 2 (mean and standard deviation) for genotype effects.
Default is \code{c(mean = 5, sd = 1)}.}

\item{effectEnv}{A numeric vector of length 2 (mean and standard deviation) for environment effects.
Default is \code{c(mean = 8, sd = sqrt(2))}.}

\item{k}{A numeric vector of weights for each principal component. Default is \code{rep(1, Nenv)}.}
}
\value{
A data frame containing the simulated GEI data with the following columns:
\describe{
  \item{gen}{Genotype identifier (factor).}
  \item{env}{Environment identifier (factor).}
  \item{rep}{Replication number (factor, default is 1).}
  \item{yield}{Simulated response variable.}
}
}
\description{
This function simulates a new dataset for genotype-environment interaction (GEI) analysis.
It generates data based on a global mean, genotype effects, environment effects, and random
multiplicative effects via singular value decomposition.
}
\examples{
# Simulate data with default parameters
sim_data <- sim.amb(seed = 123)
head(sim_data)

}
